<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.thejuki.kformmasterexample">

    <application
        android:requestLegacyExternalStorage="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:fullBackupContent="true"
        android:name="androidx.multidex.MultiDexApplication"
        tools:ignore="GoogleAppIndexingWarning"
        tools:targetApi="s"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FullscreenFormActivity" />
        <activity android:name=".PartialScreenFormActivity" />
        <activity android:name=".LoginFormActivity" />
        <activity android:name=".FormListenerActivity" />
        <activity android:name=".FormListenerJavaActivity" />
        <activity android:name=".CustomFormActivity" />
        <activity
            android:name=".FormTabbedActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".TestFormActivity" />
    </application>

</manifest>